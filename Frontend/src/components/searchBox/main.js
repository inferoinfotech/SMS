import { SearchBox } from "./SearchBox";

export default {
  title: "components/searchBox",
  component: SearchBox,
};

export const Default = {
  args: {
    className: {},
    divClassName: {},
  },
};
